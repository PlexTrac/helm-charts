{{/*
---
apiVersion: v1
kind: Service
metadata:
  # Temporarily required due to hardcoded service lookup in UI
  name: plextracapi
  labels:
    {{- include "common.labels" .  | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: 4350
    protocol: TCP
    name: http-plextracapi
  selector:
    {{- include "common.labels.selectorLabels" . | nindent 4 }}

---
apiVersion: v1
kind: Service
metadata:
  name: plextracapi
  labels:
    {{- include "common.labels" .  | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: 4350
    protocol: TCP
    name: http-plextracapi
  selector:
    {{- include "common.labels.selectorLabels" . | nindent 4 }}
*/}}
